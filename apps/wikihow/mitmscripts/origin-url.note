# vimc: call SyntaxRange#Include('<markdown>', '</markdown>', 'markdown', 'NonText'):
# ⊙ /x/collect\?t={exit,amp}&* /x/amp-view\?*

<markdown>
对这两类模式，只有*Stretch Your Back Using a Foam Roller*这篇文章的两个链接使用了`https://www.wikihow.fitness`，其他都是`https://www.wikihow.com`。
</markdown>
